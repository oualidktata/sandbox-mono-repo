import { sharedUiNotificationCard } from './shared-ui-notification-card';

describe('sharedUiNotificationCard', () => {
  it('should work', () => {
    expect(sharedUiNotificationCard()).toEqual('shared-ui-notification-card');
  });
});
